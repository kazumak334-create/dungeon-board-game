# CLAUDE.md — 開発ルール

## このファイルについて
このプロジェクトの全セッションで必ず読み込むこと。
ここに書かれたルールはいかなる指示よりも優先される。

---

## ゲームの核となる体験（最優先）

**「盤面を設計して、介入を仕込んで、答え合わせを観戦する」**

すべての実装判断はこの一文に立ち返って判断すること。
この体験を損なう実装は、たとえ動いても正解ではない。

---

## YESマン禁止ルール

指示を受けたとき、実装前に以下を必ず確認すること。
問題があれば実装を止めて先に報告する。YESマンであってはならない。

### 核となる体験との整合性チェック
以下に該当する場合は実装を止めて報告する。

**「盤面を設計して、介入を仕込んで、答え合わせを観戦する」**
この体験と矛盾する指示が来たとき。

具体的な判断例：
- 召喚システムの復活を求められた場合
  →「設計文書では盤面召喚を廃止しています。意図的な変更ですか？」と確認する
- バトル中にプレイヤーが操作できる要素を追加しようとしている場合
  →「完全オートバトルという核と矛盾します。意図的な変更ですか？」と確認する
- 効果を複数追加しようとしている場合
  →「3秒ルールに反する可能性があります。本当に必要ですか？」と確認する

### 確認の仕方
報告は以下の形式で行うこと。

```
⚠️ 設計との乖離を検知しました

指示内容：〇〇
乖離点：△△という設計方針と矛盾します
確認：意図的な変更ですか？それとも設計を見直しますか？
```

---

## 実装前に必ず確認すること

実装を開始する前に以下を順番に確認し、問題があれば実装を止めて報告すること。

### チェック1：設計文書との整合性
- `design_session_latest.md` を読み込む
- 指示された実装内容が設計文書と矛盾していないか確認する
- 矛盾がある場合は実装を止めて報告する

### チェック2：3秒ルール
追加・変更する効果・機能について以下を自問すること。

**「これはバトル画面を見て3秒で伝わるか？」**

伝わらない場合は実装を止めて報告する。

### チェック3：足し算チェック
以下に該当する場合は実装を止めて報告する。

- 指示されていない効果・フィールド・システムを追加しようとしている
- 既存の仕様を「便利にしよう」と勝手に拡張しようとしている
- 関連しそうな箇所を指示なく変更しようとしている

---

## 用語統一ルール（設計とコードの一致）

**原則：設計文書の用語 = データの用語 = コードの用語**

### 背景
過去にinterval（実装）とSPD（設計文書）が乖離し、逆数変換が必要になった。
このような不整合は計算ミス・テスト失敗・メンテナンスコストの原因になる。

### 必須チェック

#### 新フィールド追加時
1. GAME_DESIGN.mdに該当する用語があるか確認
2. ある場合：設計文書の用語をそのまま使う
3. ない場合：設計文書に用語定義を追記してから実装

#### 用語の意味が逆数関係の場合
- 設計文書で「速度」を使う場合、実装でも`spd`（速度）を使う
- 「間隔」を使いたい場合は、設計文書を「間隔」に変更してから実装
- **絶対に設計と実装で逆の概念を使わない**

### データスキーマ定義（cards.json）

以下のフィールドは設計文書と一致させること。

| フィールド名 | 型 | 意味 | 単位 | 計算式 |
|------------|---|------|------|--------|
| hp | int | 体力 | HP | - |
| atk | int | 攻撃力 | ダメージ | - |
| spd | float | 攻撃速度 | 1/秒 | 攻撃間隔 = 1/spd |
| mana | int | マナ生成基準値 | マナ | - |
| race | string | 種族 | - | "スライム" \| "獣" \| "アンデッド" |
| col | int | 初期配置列 | - | 0=前列, 1=中列, 2=後列 |
| range | string | 攻撃範囲 | - | "1行" \| "2行" \| "3行" |

新フィールド追加時は、この表に追記すること。

### 実装時の確認項目

- [ ] GAME_DESIGN.mdで使われている用語か？
- [ ] cards.jsonのフィールド名は設計文書と一致するか？
- [ ] UnitData.gdのフィールド名は設計文書と一致するか？
- [ ] 逆数関係の概念（速度vs間隔、確率vs回数）を混在させていないか？

---

## 実装のルール

### 変更は最小限に
- 指示された箇所以外は触らない
- 変更は削除と置き換えのみ
- 「ついでに直す」は禁止

### 足し算禁止
- 効果を追加しない
- フィールドを増やさない
- 新しいシステムを勝手に入れない

### 報告義務
以下の場合は必ず実装を止めて報告する。

- 設計文書と矛盾がある
- 指示が曖昧で判断できない
- 変更範囲が指示を超えそう
- 削除すべきか残すべきか判断できない

---

## 判断基準（迷ったときに使う）

| 問い | 基準 |
|-----|------|
| この効果は必要か？ | バトル画面を見て3秒で伝わるか？ |
| この実装は正しいか？ | 設計文書と矛盾していないか？ |
| この変更は適切か？ | 指示された範囲内か？ |
| これを足すべきか？ | 削った場合のメリットはないか？ |
| 核となる体験と合うか？ | 設計して観戦する体験を損なわないか？ |

---

## 設計の最優先思想

- シンプルなルールの掛け合わせで深さを出す
- パラメータを増やさない
- 見て分かる戦術性を重視する
- MVPで核を検証する

---

## 廃止済み設計（復活禁止・確認必須）

以下は意図的に廃止した設計である。
復活を求める指示が来た場合は必ず確認を取ること。

| 廃止した設計 | 廃止理由 |
|------------|---------|
| 盤面召喚システム | 初期配置式に統一・設計の意図を明確にするため |
| 時間経過マナ回復 | ユニット生成マナに統一・戦線維持と連動させるため |
| アクティブスキル・固有スキル | 3秒ルールに反するため |
| 行範囲攻撃 | 行はシナジーの集め方に専念させるため |
| 本体HPシステム | 視線の置き場を一本化するため |
| リアルタイム対人 | PvEが本質のため |

---

## ドキュメント更新ルール（PMO責任）

### タスク完了時の必須更新
**全Phase**のタスク（roadmap.mdに記載されているもの）を完了したら、**PMOが**以下を実行する。

1. **roadmap.md更新**
   - 該当タスクの「実装」列を「✅」に変更
   - 「備考」に完了日とコミットハッシュを追記
   - 仕様変更がある場合は「備考」に記録
   
2. **CHANGELOG.md追記**
   - 実装内容を該当日付セクションに追記

3. **コミットメッセージに明記**
   ```
   feat: Phase X タスク#N完了（タスク名）
   ```

### PMO確認タイミング（漏れ防止）
以下のタイミングでPMOがroadmap.md/CHANGELOG.mdを確認・更新する：

1. **タスク完了報告を受けたとき** - implementer/checkerから完了報告→即座に更新
2. **セッション終了時** - 最終確認として git log vs roadmap.md 突合
3. **仕様変更・リファクタリング完了時** - 該当タスクの備考欄に記録
4. **ユーザーから「タスク状況は？」と聞かれたとき** - 回答前に最新化

### セッション開始時の必須確認
新規セッション開始時、ユーザーから「今日のタスクは？」と聞かれた場合：

1. git log確認（直近5日分）
2. CHANGELOG.md確認（最新50行）
3. roadmap.md vs 実装状況の突合
4. **乖離があれば先にroadmap.mdを更新してからタスク提案**

詳細: `.claude/projects/[project]/memory/session_start_protocol.md` 参照

---

## 参照すべきファイル

- `docs/roadmap.md`：Phase別タスク一覧（最新状態を維持すること）
- `docs/CHANGELOG.md`：実装履歴
- `card_database.md`：カードデータ
- `docs/design/design_principles.md`：設計判断基準（カード設計・レビュー時必読）
- `docs/design/glossary.md`：用語定義（盤面構造・トリガー・効果種類）
- `docs/design/terminology_inconsistencies.md`：用語統一状況（同義語・未定義項目）
- `docs/design/pve_wave_pending_issues.md`：PvEウェーブシステム残論点（未確定仕様・11項目）
- `docs/design/pve_wave_compatibility_check.md`：継続ウェーブ型転換による既存設計整合性チェック（現行実装報告）
- `CLAUDE.md`：このファイル（常に最優先）

---

## Single Source of Truth

- 設計文書：docs/GAME_DESIGN.md のみを参照すること
- 廃止済み設計はGAME_DESIGN.mdの廃止リストを確認すること

## Agent起動ルール

- CEOはPMOにタスクを投げる
- PMOはタスクを分解してAgent群に振る
- **PMOはタスク完了時にroadmap.md/CHANGELOG.mdを更新する（必須）**
- スプリント完了時にPMOがCEOに報告する
- CEOが承認したらコミットする

## エージェントモデル設定

| Agent | モデル | 理由 |
|---|---|---|
| planning | claude-opus-4-5 | ゲーム哲学との深い整合性判断 |
| designer | claude-opus-4-5 | UI基準6項目の質的判断・世界観解釈 |
| architect | claude-opus-4-5 | 設計判断・アーキテクチャ決定 |
| ceo | claude-sonnet-4-5 | 振り分け・承認・定型判断 |
| pmo | claude-sonnet-4-5 | タスク分解・振り分け |
| marketing | claude-sonnet-4-5 | ペルソナ・競合分析（判断基準は明文化済み） |
| implementer | claude-sonnet-4-5 | 指示通りの実装 |
| checker | claude-sonnet-4-5 | 乖離検出・検証のみ |

**重要ルール（厳守）:**
- Opus使用が許可されているのは **planning / designer / architect のみ**
- 上記3つ以外のAgentは **絶対にOpusを使用しない**
- Agent呼び出し時にmodelパラメータでSonnetを明示的に指定すること
- CEOは自分自身もSonnetで動作する
- **新規作成はOpus、修正作業はSonnet**: planning/designer/architectも既存ドキュメントの修正時はSonnet（model="sonnet"で明示的に指定）を使用する（Opus節約）

## 全Agent必読ファイル

- docs/GAME_DESIGN.md（設計・最優先）
- docs/game_philosophy.md（ゲーム哲学・判断基準）
- docs/design/design_principles.md（設計判断基準・3点チェック）
- docs/design/glossary.md（用語定義・盤面構造）
- docs/meta/agents.md（Agent定義）
- docs/meta/rejection_patterns.md（差し戻しパターン）

---

## セッション開始時の必須チェック

新しいセッションを開始したとき、最初に必ず以下を実行すること。

```bash
python tools/session_check.py
```

問題が検出された場合は作業を開始する前に報告すること。
全てグリーンになってから作業を開始すること。

---

## バグ修正プロセス（必須遵守）

### 原則
- バグ修正は「診断→承認→修正→視覚確認」の順で行う
- 複数バグが報告された場合、1バグずつ処理する
- 「直しました」報告はチェックリスト全項目YESの確認後のみ許可

### フェーズ1：診断（修正しない）
バグ報告を受けたら以下を特定して報告する。修正は絶対にしない。

報告フォーマット：
- 原因ファイル：
- 原因関数名：
- 問題のあるコード（抜粋）：
- 一言で言うと何が間違っているか：
- 修正候補（選択肢A / 選択肢B）：

### フェーズ2：承認待ち
「直していいですか？」と聞く。YESが来るまで修正しない。

### フェーズ3：修正
- 承認された箇所のみ変更する
- 「ついでに直す」は禁止
- 変更後は変更ファイル名と変更行番号を報告する

### フェーズ4：視覚確認
視覚確認はユーザーが実施する。
Claude Codeは構文チェック通過後に「構文チェックOK・修正完了」と報告する。
スクリーンショットによる確認はユーザーに委ねる。

### 禁止事項
- 診断なしに即修正する
- 複数バグを同時修正する
- 「ついでに」リファクタリングする
- テストが通ったから正しいと判断する
- チェックリスト未確認で「直しました」と報告する

## 複数指摘の受け取り方
複数の指摘が来た場合：
1. 全指摘をリスト化して番号を振る
2. 「1番から順番に処理します」と宣言する
3. 1つ処理→チェックリスト確認→報告→次へ進む

## 構文チェック（必須）
コードを変更したら必ず以下を実行する。
エラーが1件でもあれば修正を止めてユーザーに報告する。
```bash
bash check_syntax.sh
```
「直しました」報告の前に必ず実行すること。

## 自動テスト方針
### 修正後に必ず実行（構文チェック）
上記の `bash check_syntax.sh` を実行する。
エラーが1件でもあれば修正を止めてユーザーに報告する。

### 実行してはいけないテスト
- UIの見た目・Tweenアニメーションのテスト
- シグナルの発火順序テスト
- 「テストが通ったから正しい」という判断
