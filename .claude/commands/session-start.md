---
description: Start a safe Dungeon Board Game development session
---

# Session Start

You are starting work on the Godot project `dungeon-board-game`.

Run and report the important results:

1. `python tools/session_check.py`
2. `git status --short`
3. Review `docs/meta/claudecode_token_budget.md`.
4. Review the current active roadmap area in `docs/roadmap.md` with grep/heading search only.
5. For Econ MVP tasks, review only relevant sections of `docs/GAME_DESIGN_V0_2_MVP.md` and `docs/requirements/REQUIREMENTS_V0_2_MVP.md`.

Before proposing implementation, state:

- current active sprint or feature area
- changed files already present in the worktree
- whether the task conflicts with the Single Source of Truth
- the smallest safe next step

Do not edit files during this command unless the user explicitly asks for implementation.
