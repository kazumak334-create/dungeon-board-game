# UIColors.gd
# 画面共通の色定数（autoload）
extends Node

# 既存の定数（Title.gd / DeckPrep.gd用）
const COLOR_BG          := Color(0.08, 0.08, 0.12)
const COLOR_PANEL       := Color(0.13, 0.13, 0.2)
const COLOR_BORDER      := Color(0.3, 0.3, 0.4)
const COLOR_TITLE       := Color(0.9, 0.85, 0.6)
const COLOR_TEXT        := Color(0.8, 0.8, 0.8)
const COLOR_SELECTED    := Color(0.9, 0.75, 0.3)
const COLOR_DIM         := Color(0.5, 0.5, 0.5)
const COLOR_PICKED      := Color(0.4, 0.6, 0.4)
const COLOR_GLOW        := Color(0.9, 0.75, 0.3, 0.3)
const COLOR_SIDE_PANEL  := Color(0.1, 0.1, 0.15)
const COLOR_PANEL_DARK  := Color(0.05, 0.05, 0.08)
const COLOR_OVERLAY     := Color(0, 0, 0, 0.4)
const COLOR_SUBTITLE    := Color(0.7, 0.75, 0.85)
const COLOR_BTN_BG      := Color(0.15, 0.15, 0.22)
const COLOR_BTN_HOVER   := Color(0.2, 0.2, 0.3)

# 新規定数（InitialCardPick.gd用）
const BG          := Color(0.08, 0.08, 0.12)
const PANEL       := Color(0.13, 0.13, 0.2)
const BORDER      := Color(0.3, 0.3, 0.4)
const TITLE       := Color(0.9, 0.85, 0.6)
const TEXT        := Color(0.8, 0.8, 0.8)
const SELECTED    := Color(0.9, 0.75, 0.3)
const DIM         := Color(0.5, 0.5, 0.5)
const PICKED      := Color(0.4, 0.6, 0.4)
const GLOW        := Color(0.9, 0.75, 0.3, 0.3)
